# ============================================================
# InsightLearn Environment Configuration Template
# ============================================================
# Security Audit: 2024-12-22
# CVE Status: CLEAN (Score 10/10)
#
# INSTRUCTIONS:
# 1. Copy this file to .env: cp .env.example .env
# 2. Run the secrets generator: ../k8s/secrets/scripts/generate-secrets.sh
# 3. Or manually replace all CHANGE_ME values with secure secrets
#
# SECURITY REQUIREMENTS:
# - All passwords must be 24+ characters with mixed case, numbers, symbols
# - JWT_SECRET_KEY must be 64+ characters (use: openssl rand -base64 64)
# - Never commit .env to version control
# - Rotate secrets every 90 days
#
# ============================================================

# ===================
# DATABASE - SQL Server
# ===================
# Minimum 24 chars, must include: uppercase, lowercase, number, special char
# Generate with: openssl rand -base64 32 | tr -d '\n'
MSSQL_SA_PASSWORD=CHANGE_ME_GENERATE_WITH_OPENSSL_32_CHARS

# ===================
# DATABASE - MongoDB
# ===================
# Generate with: openssl rand -base64 32 | tr -d '\n'
MONGODB_PASSWORD=CHANGE_ME_GENERATE_WITH_OPENSSL_32_CHARS

# ===================
# CACHE - Redis
# ===================
# Generate with: openssl rand -base64 32 | tr -d '\n'
REDIS_PASSWORD=CHANGE_ME_GENERATE_WITH_OPENSSL_32_CHARS

# ===================
# AUTHENTICATION - JWT
# ===================
# CRITICAL: Must be 64+ characters of high-entropy random data
# Generate with: openssl rand -base64 64 | tr -d '\n'
JWT_SECRET_KEY=CHANGE_ME_MINIMUM_64_CHARS_HIGH_ENTROPY_OPENSSL_RAND_BASE64
JWT_ISSUER=InsightLearn.Api
JWT_AUDIENCE=InsightLearn.Users
JWT_EXPIRATION_DAYS=7

# ===================
# ADMIN ACCOUNT
# ===================
ADMIN_EMAIL=admin@insightlearn.cloud
# Generate with: openssl rand -base64 24 | tr -d '\n'
ADMIN_PASSWORD=CHANGE_ME_GENERATE_WITH_OPENSSL_24_CHARS
ADMIN_FIRSTNAME=Admin
ADMIN_LASTNAME=User

# ===================
# ENCRYPTION KEYS
# ===================
# Generate with: openssl rand -base64 32 | tr -d '\n'
ENCRYPTION_MASTER_KEY=CHANGE_ME_GENERATE_WITH_OPENSSL_32_CHARS
VIDEO_ENCRYPTION_KEY=CHANGE_ME_GENERATE_WITH_OPENSSL_32_CHARS

# ===================
# GOOGLE OAUTH (Optional)
# ===================
# Get from: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# ===================
# STRIPE (Optional)
# ===================
# Get from: https://dashboard.stripe.com/apikeys
STRIPE_PUBLISHABLE_KEY=
STRIPE_SECRET_KEY=

# ===================
# SECURITY SETTINGS
# ===================
COOKIE_SECURE_POLICY=Always
COOKIE_SAME_SITE=Strict
COOKIE_HTTP_ONLY=true

# ===================
# ENVIRONMENT
# ===================
ASPNETCORE_ENVIRONMENT=Production
BLAZOR_DETAILED_ERRORS=false

# ===================
# LOGGING (Production - minimal for security)
# ===================
LOGGING_LEVEL_DEFAULT=Warning
LOGGING_LEVEL_MICROSOFT=Warning
LOGGING_LEVEL_SYSTEM=Warning